name: Deployning API_SERVESES 

on:
    push:
        branch: 
            - master


jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout repo 
          uses: actions/checkout@v2  
        - name: finding ssh
          run: |
            ls -a
            echo Key="${{secrets.SSH_PRIVATE_KEY}}" >> sec
        - name: connecting 
          run: |
            ssh -i /home/runner/ainfras/ainfras/sec dev@35.221.63.71
            echo "hello"
          

        
